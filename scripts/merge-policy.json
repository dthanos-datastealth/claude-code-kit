{
  "format_version": 1,
  "description": "Per-key merge policy for ~/.claude/settings.json. Consumed by scripts/intelligent-settings-merge.py. Replaces the hard-coded REPLACE behavior in the deprecated scripts/merge-settings.py.",
  "policies": {
    "env": {
      "strategy": "union_dict",
      "winner_on_conflict": "user",
      "rationale": "Kit ships broadly-needed env defaults (UV_NATIVE_TLS=1 for corporate-TLS proxies); user-set vars (e.g., NODE_EXTRA_CA_CERTS) must survive upgrade."
    },
    "enabledPlugins": {
      "strategy": "union_dict",
      "winner_on_conflict": "user",
      "rationale": "User may have manually enabled plugins the kit doesn't ship (e.g., sourcegraph); upgrade must not disable them. Kit adds new entries but never removes user entries."
    },
    "extraKnownMarketplaces": {
      "strategy": "union_dict",
      "winner_on_conflict": "kit",
      "rationale": "User-added marketplaces (e.g. an internal org marketplace) must survive an upgrade untouched, which the union gives. But for a marketplace the KIT ships, the kit's declaration wins: the entry carries the release channel as a `ref`, and if the user's older copy won, the channel could never move. That is not theoretical \u2014 `claude plugin marketplace add owner/repo@ref` is refused when settings still declares the marketplace without that ref, so user-wins made the prerelease channel unreachable for every existing install. To point a kit marketplace somewhere else, register it under a different name."
    },
    "effortLevel": {
      "strategy": "scalar_user_wins_if_set",
      "rationale": "If user has explicitly set effortLevel, preserve. If absent, take kit default."
    }
  },
  "default_strategy_for_unlisted_keys": {
    "strategy": "preserve_user",
    "rationale": "Any top-level key not in this policy is treated as user-owned and untouched by upgrade. Kit never introduces new top-level keys without adding them here first."
  }
}
